import { IEditorModel } from "src/editor/model/editorModel";

/**
 * An interface only for {@link EditorViewModel}.
 */
 export interface IEditorViewModel {

}

/**
 * @class // TODO
 */
export class EditorViewModel implements IEditorViewModel {

    // [field]

    // [event]

    // [constructor]

    constructor(
        model: IEditorModel,
    ) {

    }

    // [public methods]

    // [private helper methods]

}