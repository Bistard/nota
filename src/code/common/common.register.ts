
/**
 * This file aggregates all registration files. It centralizes the shared 
 * registrations applicable to either the main process or the renderer processes.
 */

// configuration register
import 'src/code/browser/configuration.register';
import 'src/code/browser/workbench/parts/sideView/configuration.register';